<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===================================================== -->
    <!-- Permissions -->
    <!-- ===================================================== -->
    
    <!-- Required for AdMob to fetch ads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".AttendMateApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendMate">



        <!-- ===================================================== -->
        <!-- ENTRY / LAUNCHER -->
        <!-- ===================================================== -->
        <activity
            android:name=".ui.entry.EntryActivity"
            android:exported="true"
            android:theme="@style/Theme.AttendMate.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===================================================== -->
        <!-- MAIN -->
        <!-- ===================================================== -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AttendMate" />

        <!-- ===================================================== -->
        <!-- AUTH -->
        <!-- ===================================================== -->
        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" android:exported="false" />

        <!-- ===================================================== -->
        <!-- SETUP -->
        <!-- ===================================================== -->
        <activity android:name=".ui.setup.SubjectSetupActivity" android:exported="false" />
        <activity android:name=".ui.timetable.setup.TimetableSetupActivity" android:exported="false" />

        <!-- ===================================================== -->
        <!-- SUBJECT / ATTENDANCE -->
        <!-- ===================================================== -->
        <activity android:name=".ui.subjects.ManageSubjectsActivity" android:exported="false" />
        <activity android:name=".ui.attendance.AttendanceListActivity" android:exported="false" />
        <activity android:name=".ui.attendance.AddAttendanceActivity" android:exported="false" />
        <activity android:name=".ui.attendance.EditAttendanceActivity" android:exported="false" />

        <!-- ===================================================== -->
        <!-- ANALYTICS / SETTINGS / INTEGRATION -->
        <!-- ===================================================== -->
        <activity android:name=".ui.analytics.AnalyticsActivity" android:exported="false" />
        <activity android:name=".ui.settings.SettingsActivity" android:exported="false" />
        <activity android:name=".ui.settings.CollegeSyncActivity" android:exported="false" />

        <!-- ===================================================== -->
        <!-- WIDGET -->
        <!-- ===================================================== -->
        <receiver
            android:name=".ui.widget.TotalAttendanceWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/total_attendance_widget_info" />
        </receiver>

        <!-- ===================================================== -->
        <!-- LECTURE NOTIFICATIONS -->
        <!-- ===================================================== -->


        <!-- ===================================================== -->
        <!-- DAY CONFIRMATION -->
        <!-- ===================================================== -->



        <!-- Restore alarms after reboot -->

        <!-- ===================================================== -->
        <!-- LEGACY -->
        <!-- ===================================================== -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="false"
            tools:ignore="UnusedActivity" />

        <activity
            android:name=".ui.friends.FriendsActivity"
            android:exported="false" />
        <activity android:name=".ui.friends.FriendProfileActivity"
            android:exported="false" />

        <service
            android:name=".ui.settings.CollegeSyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
